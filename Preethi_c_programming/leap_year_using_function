#include<stdio.h>
int leap(int x);
int x,n;
void main()

{
   
    printf("enter the year:");
    scanf("%d",&x);
    printf("%d",leap(x));
}
    int leap( int x)
   
{
    if(x%4==0 || x%100==0 && x%400==0)
    {
    printf("leap year");
    }
    else
    {
    printf(" not a leap year");
    }
}
